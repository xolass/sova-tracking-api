import express from 'express';

const router = express.Router();
/* 
socketer.on('location', (data) => { console.log(data) })
socketer.on('hi', () => { console.log('hello!') })
 */
export default router;
